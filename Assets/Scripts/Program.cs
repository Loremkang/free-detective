using System;

namespace free_detective
{
    class Program
    {
        static void Main(string[] args)
        {
            // out of date
            // Reader reader = new Reader();
            // Item EP1 = new Item("EP1", "EP1Description", "关卡", null);
            // Data.globalItemMap["EP1"] = EP1;
            // reader.read("./scripts/EP1/objects1.csv", EP1);
            // Driver gameDriver = new Driver();
            // gameDriver.Start();
        }
    }
}
